export default function HomePage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center p-24">
      <div className="text-center">
        <h1 className="text-4xl font-bold tracking-tight sm:text-6xl">
          Welcome to {{PROJECT_NAME}}
        </h1>
        <p className="mt-6 text-lg leading-8 text-gray-600">
          Your Next.js app is ready. Start building in <code className="font-mono">features/</code>
        </p>
        <div className="mt-10 flex items-center justify-center gap-x-6">
          <a
            href="/docs"
            className="rounded-md bg-blue-600 px-3.5 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-blue-500"
          >
            Get started
          </a>
          <a href="/api/health" className="text-sm font-semibold leading-6 text-gray-900">
            Check API <span aria-hidden="true">→</span>
          </a>
        </div>
      </div>
    </main>
  );
}
